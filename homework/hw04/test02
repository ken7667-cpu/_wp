function sumArray(numbers) {
  let total = 0;
  // 使用 for 迴圈 (Loop) 走訪陣列
  for (let i = 0; i < numbers.length; i++) {
    total += numbers[i];
  }
  return total;
}
console.log("練習 2:", sumArray([1, 2, 3, 4, 5])); // 輸出: 15
